<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" /> <!-- Ajusta el radio de la esquina según corresponda -->
    <solid android:color="@android:color/white" /> <!-- Color de fondo del CardView -->
    <padding android:left="4dp"
        android:right="4dp"
        android:top="8dp"
        android:bottom="4dp" /> <!-- Ajusta el espaciado para la sombra -->
    <corners android:radius="8dp" />
    <stroke
        android:width="1dp"
        android:color="#dddddd" /> <!-- Color del borde opcional -->
    <gradient
        android:startColor="#CCCCCC"
        android:endColor="#CCCCCC"
        android:angle="90" />
</shape>
